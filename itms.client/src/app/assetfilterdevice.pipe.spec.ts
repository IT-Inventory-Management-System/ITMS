import { AssetfilterdevicePipe } from './assetfilterdevice.pipe';

describe('AssetfilterdevicePipe', () => {
  it('create an instance', () => {
    const pipe = new AssetfilterdevicePipe();
    expect(pipe).toBeTruthy();
  });
});
