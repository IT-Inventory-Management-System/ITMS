import { FilterSearchListPipe } from './filter-search-list.pipe';

describe('FilterSearchListPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterSearchListPipe();
    expect(pipe).toBeTruthy();
  });
});
